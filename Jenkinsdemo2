pipeline {
agent any 
     stages{
       stage("build") {
          steps {
            num1 = '25'
            num2 = '33'
          add = $num1+$num2
          echo "${add}"
          }
       }
     }
}
