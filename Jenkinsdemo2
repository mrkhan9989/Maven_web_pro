pipeline {
agent any 
     stages{
       stage("build") {
          steps {
            script {
            def num1 = 5
            def num = 6
             def sum = num1 + num2
              echo "${sum}"
                }
          }
       }
     }
}
