pipeline {
agent any 
     stages{
       stage("build") {
          steps {
            script {
             num1 = 5
             num = 6
              sum = num1 + num2
              echo "$sum"
                }
          }
       }
     }
}
